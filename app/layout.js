import './globals.css';

export const metadata = {
  title: 'Medicines Registration System',
  description: 'Pharmaceutical medicines registration system',
};

export default function RootLayout({ children }) {
  return children;
}

